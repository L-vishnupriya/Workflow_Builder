# Angular Integration Demo

This is a runnable Angular app configured to consume `c1x-workflow-builder` via local linking.

## Run With npm link

1. From project root:

   npm run build
   npm link

2. In this folder:

   npm install
   npm link c1x-workflow-builder
   npm start

3. Open the URL printed by Angular dev server (usually http://localhost:4200).
